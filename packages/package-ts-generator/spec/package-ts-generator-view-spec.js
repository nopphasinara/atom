'use babel';

import PackageTsGeneratorView from '../lib/package-ts-generator-view';

describe('PackageTsGeneratorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
