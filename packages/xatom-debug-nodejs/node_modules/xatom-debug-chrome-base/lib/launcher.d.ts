export interface Page {
    type: string;
    url: string;
    webSocketDebuggerUrl?: string;
}
export declare type Pages = Array<Page>;
export declare class ChromeDebuggingProtocolLauncher {
    portNumber: number;
    hostName: string;
    private process;
    private maxAttempts;
    private launched;
    private events;
    unquote(value: string): string;
    quote(value: string): string;
    didStop(cb: any): void;
    didFail(cb: any): void;
    didReceiveOutput(cb: any): void;
    didReceiveError(cb: any): void;
    emitFailure(text: string): void;
    getLauncherArguments(): Array<string>;
    getBinaryPath(): string;
    getProcessOptions(): Object;
    stop(): void;
    start(): Promise<string>;
    getPages(): Promise<Page[]>;
    findPageUrl(page: any): boolean;
    findSocketUrl(pages: any): Promise<string>;
    getSocketUrl(): Promise<string | void>;
}
