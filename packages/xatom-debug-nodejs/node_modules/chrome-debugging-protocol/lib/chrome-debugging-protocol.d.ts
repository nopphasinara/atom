import { Domains } from './domain/index';
export interface ChromeDebuggingProtocolOptions {
    log?: boolean;
}
export declare class ChromeDebuggingProtocol {
    private socketUrl;
    private socket;
    private event;
    private requester;
    private log;
    constructor(socketUrl: string, options?: ChromeDebuggingProtocolOptions);
    didClose(cb: any): void;
    didReceiveError(cb: any): void;
    disconnect(): void;
    connect(): Promise<Domains>;
}
